// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: assistant/copilot/chat.proto

package copilot

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	ChatService_Chat_FullMethodName = "/assistant.copilot.v1.ChatService/Chat"
)

// ChatServiceClient is the client API for ChatService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// *
// ChatService 设计说明文档 (AI Agent Conversation Protocol)
//
// 本服务定义了一个通用、简单且可扩展的多人、多 Agent 会话协议。核心设计理念如下：
//
// 1. 会话与多分支 (Sessions & Conversation Branches)
//   - `session_id`: 宏观层级的大房间（类似一个群聊），承载多实体（人/Agent）参与。
//   - `conversation_id`: 微观层级的话题分支。允许在同一个房间内开辟并行的讨论或子任务，彼此隔离上下文。
//
// 2. 消息异步追踪与幂等 (Idempotency & Message Tracking)
//   - `req_id`: 客户端生成的临时流水号。因为是双向流，用于将服务端的首次响应(Ack)跟客户端某次具体请求对应起来。
//   - `msg_id`: 服务端全局生成的唯一消息ID。客户端拿到此ID后，可用作后续撤回、取消(Cancel)、重试的唯一凭证。
//
// 3. 并发双向流模型 (Bidirectional Streaming)
//   - 客户端可以随时(无需等待上一次响应)发送多个任务(`PostMessage`)或控制指令(`Control`)。
//   - 服务端可以在一条持续的流中交叉复用事件：比如先下发 `Ack` 确定任务分配，然后持续下发 `MessageDelta` 回传打字机效果。
//
// 4. 强类型业务承载 (Rich Typed Payload)
//   - 舍弃跨语言难以处理的无约束 `map<string, object>` 结构。
//   - 采用标准化的 `repeated Attachment` 来传输附件资源。
//   - 采用 `google.protobuf.Struct` 承载前端UI状态或大模型语境所需的灵活 `context` (如选中代码块等)。
//
// 5. 增量渲染规范 (Delta Principle)
//   - 参照业界大模型流式生成标准，使用 `content_delta` 表示增量文字片段，而不是替换整体。
type ChatServiceClient interface {
	// 核心双向流接口：客户端可连续发消息/发指令，服务端可持续推送状态/增量结果
	Chat(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[ChatRequest, ChatEvent], error)
}

type chatServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewChatServiceClient(cc grpc.ClientConnInterface) ChatServiceClient {
	return &chatServiceClient{cc}
}

func (c *chatServiceClient) Chat(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[ChatRequest, ChatEvent], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &ChatService_ServiceDesc.Streams[0], ChatService_Chat_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[ChatRequest, ChatEvent]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type ChatService_ChatClient = grpc.BidiStreamingClient[ChatRequest, ChatEvent]

// ChatServiceServer is the server API for ChatService service.
// All implementations must embed UnimplementedChatServiceServer
// for forward compatibility.
//
// *
// ChatService 设计说明文档 (AI Agent Conversation Protocol)
//
// 本服务定义了一个通用、简单且可扩展的多人、多 Agent 会话协议。核心设计理念如下：
//
// 1. 会话与多分支 (Sessions & Conversation Branches)
//   - `session_id`: 宏观层级的大房间（类似一个群聊），承载多实体（人/Agent）参与。
//   - `conversation_id`: 微观层级的话题分支。允许在同一个房间内开辟并行的讨论或子任务，彼此隔离上下文。
//
// 2. 消息异步追踪与幂等 (Idempotency & Message Tracking)
//   - `req_id`: 客户端生成的临时流水号。因为是双向流，用于将服务端的首次响应(Ack)跟客户端某次具体请求对应起来。
//   - `msg_id`: 服务端全局生成的唯一消息ID。客户端拿到此ID后，可用作后续撤回、取消(Cancel)、重试的唯一凭证。
//
// 3. 并发双向流模型 (Bidirectional Streaming)
//   - 客户端可以随时(无需等待上一次响应)发送多个任务(`PostMessage`)或控制指令(`Control`)。
//   - 服务端可以在一条持续的流中交叉复用事件：比如先下发 `Ack` 确定任务分配，然后持续下发 `MessageDelta` 回传打字机效果。
//
// 4. 强类型业务承载 (Rich Typed Payload)
//   - 舍弃跨语言难以处理的无约束 `map<string, object>` 结构。
//   - 采用标准化的 `repeated Attachment` 来传输附件资源。
//   - 采用 `google.protobuf.Struct` 承载前端UI状态或大模型语境所需的灵活 `context` (如选中代码块等)。
//
// 5. 增量渲染规范 (Delta Principle)
//   - 参照业界大模型流式生成标准，使用 `content_delta` 表示增量文字片段，而不是替换整体。
type ChatServiceServer interface {
	// 核心双向流接口：客户端可连续发消息/发指令，服务端可持续推送状态/增量结果
	Chat(grpc.BidiStreamingServer[ChatRequest, ChatEvent]) error
	mustEmbedUnimplementedChatServiceServer()
}

// UnimplementedChatServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedChatServiceServer struct{}

func (UnimplementedChatServiceServer) Chat(grpc.BidiStreamingServer[ChatRequest, ChatEvent]) error {
	return status.Errorf(codes.Unimplemented, "method Chat not implemented")
}
func (UnimplementedChatServiceServer) mustEmbedUnimplementedChatServiceServer() {}
func (UnimplementedChatServiceServer) testEmbeddedByValue()                     {}

// UnsafeChatServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ChatServiceServer will
// result in compilation errors.
type UnsafeChatServiceServer interface {
	mustEmbedUnimplementedChatServiceServer()
}

func RegisterChatServiceServer(s grpc.ServiceRegistrar, srv ChatServiceServer) {
	// If the following call pancis, it indicates UnimplementedChatServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&ChatService_ServiceDesc, srv)
}

func _ChatService_Chat_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(ChatServiceServer).Chat(&grpc.GenericServerStream[ChatRequest, ChatEvent]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type ChatService_ChatServer = grpc.BidiStreamingServer[ChatRequest, ChatEvent]

// ChatService_ServiceDesc is the grpc.ServiceDesc for ChatService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var ChatService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "assistant.copilot.v1.ChatService",
	HandlerType: (*ChatServiceServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Chat",
			Handler:       _ChatService_Chat_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "assistant/copilot/chat.proto",
}
