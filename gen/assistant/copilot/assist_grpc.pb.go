// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: assistant/copilot/assist.proto

package copilot

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	AssistService_Assist_FullMethodName = "/assistant.copilot.v1.AssistService/Assist"
)

// AssistServiceClient is the client API for AssistService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// *
// AssistService: 隐式智能助手 (Implicit Copilot) 协议设计文档
//
// 适用场景: 写代码补全、文档续写、沉浸式文献阅读提示、绘图工具参数智能推荐、金融复盘研判等"非对话"场景。
//
// 核心设计理念：
//
// 1. 状态与动作解耦 (Context vs Action)
//   - 在隐式场景中，客户端不主动下发指令，而是持续保持同步。为了节省带宽与 Token，上行走双轨制：
//   - `ContextSync`: 宏观状态快照（例如：切换了 App、打开了某代码文件/K线图，包含附近大量上下文）。低频长文。
//   - `UserAction`: 微观行为碎片（例如：敲击字符、点击按钮、滚动屏幕）。高频短文，服务端通过拼接上下文进行推断。
//
// 2. 泛型建议的下行投递 (Three modes of Interventions)
//   - 根据用户所处的场景，服务端主动下发不同形态的“帮助”：
//   - `InlineCompletion`: 行内灰色虚拟文本（用于文字输入、写代码等续写型推断）。
//   - `Insight`: 悬浮/侧边洞察（比如文档单词拼写纠错划红线，或是分析某只股票财报给出的划线提示）。
//   - `Command`: 界面宏指令（AI决定不需要提示，直接帮你完成。例如前端收到后自动弹出创建一个日历日程的窗口）。
//
// 3. 闭环反馈机制 (Mandatory Feedback Loop)
//   - 无声模式下，AI很难感知效果。任何下发给客户端的建议都带有一个全局唯一的 `suggestion_id`。
//   - 客户端在用户做出决定后（按 Tab 接纳、无视并继续打字、只复制部分），必须上报 `Feedback`。
//   - 形成闭环，作为大模型 RLHF (强化学习微调) 与产品商业化埋点最重要的指标。
type AssistServiceClient interface {
	// 核心双向流：客户端持续上报上下文和行为，服务端适时下发建议和操作指令
	Assist(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[AssistRequest, AssistEvent], error)
}

type assistServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewAssistServiceClient(cc grpc.ClientConnInterface) AssistServiceClient {
	return &assistServiceClient{cc}
}

func (c *assistServiceClient) Assist(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[AssistRequest, AssistEvent], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &AssistService_ServiceDesc.Streams[0], AssistService_Assist_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[AssistRequest, AssistEvent]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AssistService_AssistClient = grpc.BidiStreamingClient[AssistRequest, AssistEvent]

// AssistServiceServer is the server API for AssistService service.
// All implementations must embed UnimplementedAssistServiceServer
// for forward compatibility.
//
// *
// AssistService: 隐式智能助手 (Implicit Copilot) 协议设计文档
//
// 适用场景: 写代码补全、文档续写、沉浸式文献阅读提示、绘图工具参数智能推荐、金融复盘研判等"非对话"场景。
//
// 核心设计理念：
//
// 1. 状态与动作解耦 (Context vs Action)
//   - 在隐式场景中，客户端不主动下发指令，而是持续保持同步。为了节省带宽与 Token，上行走双轨制：
//   - `ContextSync`: 宏观状态快照（例如：切换了 App、打开了某代码文件/K线图，包含附近大量上下文）。低频长文。
//   - `UserAction`: 微观行为碎片（例如：敲击字符、点击按钮、滚动屏幕）。高频短文，服务端通过拼接上下文进行推断。
//
// 2. 泛型建议的下行投递 (Three modes of Interventions)
//   - 根据用户所处的场景，服务端主动下发不同形态的“帮助”：
//   - `InlineCompletion`: 行内灰色虚拟文本（用于文字输入、写代码等续写型推断）。
//   - `Insight`: 悬浮/侧边洞察（比如文档单词拼写纠错划红线，或是分析某只股票财报给出的划线提示）。
//   - `Command`: 界面宏指令（AI决定不需要提示，直接帮你完成。例如前端收到后自动弹出创建一个日历日程的窗口）。
//
// 3. 闭环反馈机制 (Mandatory Feedback Loop)
//   - 无声模式下，AI很难感知效果。任何下发给客户端的建议都带有一个全局唯一的 `suggestion_id`。
//   - 客户端在用户做出决定后（按 Tab 接纳、无视并继续打字、只复制部分），必须上报 `Feedback`。
//   - 形成闭环，作为大模型 RLHF (强化学习微调) 与产品商业化埋点最重要的指标。
type AssistServiceServer interface {
	// 核心双向流：客户端持续上报上下文和行为，服务端适时下发建议和操作指令
	Assist(grpc.BidiStreamingServer[AssistRequest, AssistEvent]) error
	mustEmbedUnimplementedAssistServiceServer()
}

// UnimplementedAssistServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAssistServiceServer struct{}

func (UnimplementedAssistServiceServer) Assist(grpc.BidiStreamingServer[AssistRequest, AssistEvent]) error {
	return status.Errorf(codes.Unimplemented, "method Assist not implemented")
}
func (UnimplementedAssistServiceServer) mustEmbedUnimplementedAssistServiceServer() {}
func (UnimplementedAssistServiceServer) testEmbeddedByValue()                       {}

// UnsafeAssistServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AssistServiceServer will
// result in compilation errors.
type UnsafeAssistServiceServer interface {
	mustEmbedUnimplementedAssistServiceServer()
}

func RegisterAssistServiceServer(s grpc.ServiceRegistrar, srv AssistServiceServer) {
	// If the following call pancis, it indicates UnimplementedAssistServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&AssistService_ServiceDesc, srv)
}

func _AssistService_Assist_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(AssistServiceServer).Assist(&grpc.GenericServerStream[AssistRequest, AssistEvent]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AssistService_AssistServer = grpc.BidiStreamingServer[AssistRequest, AssistEvent]

// AssistService_ServiceDesc is the grpc.ServiceDesc for AssistService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var AssistService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "assistant.copilot.v1.AssistService",
	HandlerType: (*AssistServiceServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Assist",
			Handler:       _AssistService_Assist_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "assistant/copilot/assist.proto",
}
